server:
  port: 9999

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/test
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: false
      generate-ddl: false
      hibernate.ddl-auto: validate

logging:
  level:
    org:
      hibernate:
        type: trace
